# Cole local config — copy to config/cole.env (gitignored).
# Easiest: ./scripts/setup-cole-env.sh (reads LEARNING_KB_API_TOKEN from GitHub repo variable)

LEARNING_KB_API_URL=https://kb-api.keyflo.ai
LEARNING_KB_API_TOKEN=

# Manual:
#   cp config/cole.env.example config/cole.env
#   paste token from GitHub Settings → Variables → LEARNING_KB_API_TOKEN
#   set -a && source config/cole.env && set +a
#   python scripts/query_api.py "which courses cover copywriting?"
